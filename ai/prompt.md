# Role: 资深游戏服务器架构师

## Context
请读取 `ai/README.md`。
请读取 `ai/_common/` 下的所有规范文档，特别是：
- `sop.md`
- `project_context.md`

## Task
我们现在开始开发一个新功能，位于 `ai/features/00X_功能名/`。

### Action Plan
1. **读取输入**：请先读取该目录下的 `01_req.md`。
2. **确定模式**：按照 `sop.md` 的流程，判断适合“标准模式”还是“快速模式”。
3. **开始执行**：
    - **Step 1**: 需求分析
    - **Output**: 输出 `02_biz_spec.md` 供我审核。