
***

# 游戏服务端新功能开发流程指南

本文档旨在指导AI协同人类开发者，基于现有服务端架构开发新功能。文档规定了从需求分析到最终测试验收的完整标准作业程序（SOP）。

## 1. 需求分析与理解
*   **输入**：策划提供的原始开发文档（通常为.doc格式，包含文字、图片、图表等）。
*   **执行**：AI需深度阅读并初步分析原始文档，理解功能背景、核心玩法及详细规则。
*   **目标**：建立对新功能的宏观认知，为后续规则拆解做准备。

## 2. 业务规则标准化（Business Spec）
*   **输入**：策划原始文档。
*   **执行**：
    *   **AI初稿**：根据开发文档，将图文信息转化为纯文本的Markdown格式规则文档。要求逻辑清晰、细节详尽，覆盖功能的方方面面。此文档**不包含图片**，仅保留文字描述。
    *   **人工审核**：人类开发者对AI生成的Markdown文档进行审核与补充，双方确认所有细则。
*   **输出**：`.md` 格式的**业务规则文档**。
*   **目标**：确立唯一的业务逻辑标准，供后续程序设计引用。

## 3. 程序设计与技术方案（Technical Design）
在此阶段，AI与人类协同制定技术实现方案。最终将以下内容汇总为一个强调程序设计的 `.md` 文件，作为编码基准。

### 3.1 静态数据设计
*   **规则参考**：`config.md`
*   **内容**：依据业务规则文档，设计能够表达功能逻辑的数据结构（表结构），包括新增的全局常量配置等。

### 3.2 通讯协议设计
*   **规则参考**：`protocol.md`
*   **内容**：设计客户端与服务端的交互协议（Request/Response），明确字段定义，确保能支撑完整的玩法交互。

### 3.3 数据结构与缓存设计
*   **规则参考**：`AI.md`
*   **内容**：制定玩家模块的数据缓存类（如 `QuestModule`）及功能实现所需的基础辅助类。

### 3.4 业务流程与逻辑设计
*   **规则参考**：`AI.md`
*   **内容**：
    *   定义关键类的方法与接口。
    *   描述静态数据表的使用方式。
    *   梳理业务流转逻辑（可用流程图或伪代码描述），明确上下文关系。

## 4. 功能编码实现
*   **输入**：步骤3生成的程序设计文档。
*   **执行**：编写具体的Java逻辑代码。包括创建新类、修改现有类、实现接口等。
*   **输出**：完成编译的源代码。

## 5. 构建、部署与测试验收
此步骤为闭环验证过程，包含编译、启动、模拟测试与日志验收。

### 5.1 源码编译
*   **路径**：工程主目录。
*   **命令**：`mvn clean install`

### 5.2 服务端启停管理
*   **启动**：运行 `game/start.bat`，参数为 `start`。
    *   *注*：启动过程可能耗时几秒至十几秒。
*   **状态检查**：
    *   执行启动命令30秒后，检查 `game` 目录是否存在 `GameServer.pid` 文件。
    *   **存在**：表示启动成功。
    *   **不存在**：表示启动失败。需检查 `stdout.log` 文件，分析错误原因，修复代码后重新编译并启动。
*   **停止**：运行 `game/start.bat`，参数为 `stop`。

### 5.3 客户端模拟与协议测试
*   **规则参考**：`client.md`
*   **执行**：编写客户端协议模拟代码，模拟请求并处理服务端响应。
*   **逻辑编排（重要）**：
    *   **时序性**：功能通常涉及多个协议，AI需根据业务逻辑编排发送顺序。
        *   *示例*：副本玩法需先发送“请求副本数据”，获取数据缓存后，再发送“领取奖励”。
    *   **状态依赖**：后一个请求的参数可能依赖前一个请求的返回数据；若前置条件不满足（如已领取过奖励），则不应发送后续请求。
    *   **执行方式**：确定顺序后，按步骤逐一启动测试。

### 5.4 验收与日志验证
*   **验证标准**：观察日志文件中的服务端返回消息。
*   **日志结构**：
    ```text
    opType[recv]player[{5}]errorCode[{0}]id[{1}]name[{2}]content[{3}]seq[{4}]
    ```
*   **检查点**：
    1.  **协议匹配**：发送的消息ID应与返回的消息ID对应（如发送 `ShopItemBuyRequest_15000003`，日志应包含ID:15000003,protocol.md中的协议+1规则）。
    2.  **错误码（errorCode）**：必须检查 `errorCode` 的值。
        *   `0`：表示成功。
        *   `非0`：表示存在业务或系统错误。
    3.  **数据正确性**：针对特定玩法，检查 `content` 中的具体数据是否符合预期。

### 5.5 迭代闭环
*   如果发现数据返回异常或错误码非0，AI应尝试修复代码或标记问题供人工处理。
*   **循环流程**：修改代码 -> 重新编译 -> 重启服务 -> 重新测试，直至所有协议逻辑均通过验证。

***
